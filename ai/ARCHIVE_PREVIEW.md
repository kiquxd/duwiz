# Archive preview integration

duwiz presents `ArchivePreview` as a table containing path, kind, size and
method. It does not open or extract entries. A future archive-entry action must
continue treating paths as untrusted labels rather than filesystem targets.
